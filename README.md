# lab4 - React Lab

This project contains:
- A Todo app (as in the lab instructions)
- Routing pages: / (Home with Todo), /login, /news, /profile
- Protected /profile route that requires authentication (stored in localStorage)

How to run:
1. `npm install`
2. `npm start`

Login credentials:
- username: `Admin`
- password: `12345`
